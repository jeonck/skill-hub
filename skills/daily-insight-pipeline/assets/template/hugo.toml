baseURL = "{{BASE_URL}}"
languageCode = "ko-kr"
title = "{{SITE_TITLE}}"
timeZone = "Asia/Seoul"
enableRobotsTXT = true

# 자체 레이아웃만 사용 — 외부 테마 의존 없음

[taxonomies]
# 단수=복수 동일하게 두어 front matter 키(verdict:, status:)를 그대로 taxonomy로 사용
verdict = "verdict"
status = "status"
tag = "tags"

[markup.goldmark.renderer]
unsafe = true

[pagination]
pagerSize = 50
