<!--
  {{INPUT_INSTRUCTIONS_LINE1}}
  - 이 파일은 GitHub 웹 UI(저장소 페이지 → 이 파일 클릭 → 연필 아이콘 → 수정 → Commit)에서
    편집하면 됩니다. 로컬에서 git pull/push 할 필요 없습니다.
  - 형식: 코드블록(```) 안에 한 줄에 {{ITEM_LABEL}} 하나씩 적습니다.
  - 한 줄이면 포스트 1개, 여러 줄이면 줄마다 각각 포스트 1개씩 생성됩니다.
  - 이미 포스트로 만들어진 {{ITEM_LABEL}}은(는)(내용이 완전히 같으면) 다시 두어도
    건너뜁니다 — 같은 날이든 다른 날이든 상관없이 텍스트 기준으로 중복을 막습니다.
  - 다음 날 {{CRON_KST_TIME}}(KST) 자동 실행에서 새 {{ITEM_LABEL}}들을 기준으로
    포스트가 생성되어 블로그에 게시됩니다. Actions 탭에서 수동 실행도 가능합니다.
  - 코드블록 안을 완전히 비워두면, {{FALLBACK_DESCRIPTION}} 목록에서 그날 순서에
    맞는 항목을 대신 사용해 포스트를 생성합니다 (입력을 깜빡해도 게시가 끊기지 않습니다).
-->
```
{{EXAMPLE_ITEM}}
```
